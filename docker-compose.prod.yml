# Production-grade docker-compose with enhanced configuration
version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: voiceorchid-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-devuser}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-devpass}
      POSTGRES_DB: ${POSTGRES_DB:-voiceorchid}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-devuser}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - voiceorchid-network
    security_opt:
      - no-new-privileges:true

  redis:
    image: redis:7-alpine
    container_name: voiceorchid-redis
    restart: unless-stopped
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD:-}
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - voiceorchid-network
    security_opt:
      - no-new-privileges:true

  livekit:
    image: livekit/livekit-server:v1.13.6
    container_name: voiceorchid-livekit
    restart: unless-stopped
    # Host networking so WebRTC media (the UDP port range in livekit.prod.yaml)
    # binds directly on the EC2 host's public interface — required for clients
    # to actually reach the SFU; Docker's per-port NAT doesn't work for this.
    network_mode: host
    command: ["--config", "/etc/livekit.yaml", "--keys", "${LIVEKIT_API_KEY:?LIVEKIT_API_KEY must be set}: ${LIVEKIT_API_SECRET:?LIVEKIT_API_SECRET must be set}"]
    volumes:
      - ./livekit/livekit.prod.yaml:/etc/livekit.yaml:ro
    security_opt:
      - no-new-privileges:true

  api:
    build:
      context: ./AgentServer
      dockerfile: Dockerfile
    container_name: voiceorchid-api
    restart: unless-stopped
    environment:
      ENVIRONMENT: ${ENVIRONMENT:-development}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      POSTGRES_USER: ${POSTGRES_USER:-devuser}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set}
      POSTGRES_DB: ${POSTGRES_DB:-voiceorchid}
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      REDIS_HOST: redis
      REDIS_PORT: 6379
      JWT_SECRET_KEY: ${JWT_SECRET_KEY:?JWT_SECRET_KEY must be set}
      # Self-hosted LiveKit (the `livekit` service above) is reachable on the
      # host's network; point this at the EC2 host's public IP/domain (e.g.
      # ws://<ec2-ip>:7880 or wss://livekit.yourdomain.com) — it must be the
      # same address both this container and the browser can reach, since
      # it's handed straight to the browser as the room's connection URL.
      LIVEKIT_URL: ${LIVEKIT_URL:?LIVEKIT_URL must be set}
      LIVEKIT_API_KEY: ${LIVEKIT_API_KEY:?LIVEKIT_API_KEY must be set}
      LIVEKIT_API_SECRET: ${LIVEKIT_API_SECRET:?LIVEKIT_API_SECRET must be set}
      MULTI_TENANCY_ENABLED: "true"
      RATE_LIMIT_ENABLED: "true"
      AUDIT_LOG_ENABLED: "true"
      ENABLE_METRICS: "true"
    ports:
      - "${API_PORT:-8000}:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      livekit:
        condition: service_started
    networks:
      - voiceorchid-network
    security_opt:
      - no-new-privileges:true

  worker:
    build:
      context: ./AgentServer
      dockerfile: Dockerfile
    container_name: voiceorchid-worker
    restart: unless-stopped
    command: ["python", "agents/workers/entrypoint.py", "start"]
    environment:
      PYTHONPATH: .
      ENVIRONMENT: ${ENVIRONMENT:-development}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      POSTGRES_USER: ${POSTGRES_USER:-devuser}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set}
      POSTGRES_DB: ${POSTGRES_DB:-voiceorchid}
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      REDIS_HOST: redis
      REDIS_PORT: 6379
      JWT_SECRET_KEY: ${JWT_SECRET_KEY:?JWT_SECRET_KEY must be set}
      LIVEKIT_URL: ${LIVEKIT_URL:?LIVEKIT_URL must be set}
      LIVEKIT_API_KEY: ${LIVEKIT_API_KEY:?LIVEKIT_API_KEY must be set}
      LIVEKIT_API_SECRET: ${LIVEKIT_API_SECRET:?LIVEKIT_API_SECRET must be set}
      OPENAI_API_KEY: ${OPENAI_API_KEY:?OPENAI_API_KEY must be set}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      livekit:
        condition: service_started
    networks:
      - voiceorchid-network
    security_opt:
      - no-new-privileges:true

  client:
    build:
      context: ./AgentUi/agent@ui
      dockerfile: Dockerfile.prod
      args:
        VITE_APP_BASE_URL: ${VITE_APP_BASE_URL:-http://localhost:8000}
    container_name: voiceorchid-ui
    restart: unless-stopped
    ports:
      - "${CLIENT_PORT:-80}:80"
    depends_on:
      - api
    networks:
      - voiceorchid-network
    security_opt:
      - no-new-privileges:true

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local

networks:
  voiceorchid-network:
    driver: bridge
